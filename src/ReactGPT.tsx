import { RouterProvider } from "react-router";
import { router } from "./presetation/router/router";

const ReactGPT = () => {
  return <RouterProvider router={router} />;
};

export default ReactGPT;
